# 变更报告

> 05-deliver 阶段产物，记录本次任务的所有变更。

---

## 概要

- **任务目标：** [一句话]
- **执行结果：** ✅ 完成 / ⚠️ 部分完成 / ❌ 未完成
- **变更范围：** [N] 个文件修改，[M] 个文件新增，[K] 个文件删除

---

## 变更清单

| # | 文件路径 | 变更类型 | 变更说明 |
|---|----------|----------|----------|
| 1 | | 新增/修改/删除 | |

---

## 新增依赖

| 依赖名 | 版本 | 引入原因 | 验证状态 |
|--------|------|----------|----------|
| | | | ✅已验证 / ⚠️待验证 / ❌验证失败 |

（无新增依赖则写"无"）

### 依赖验证详情

> 由03-execute依赖验证门禁自动生成。

| 依赖名 | 存在性 | 版本可用 | 安全检查 | 技术栈兼容 | 操作 |
|--------|--------|----------|----------|------------|------|
| | ✅/❌ | ✅/❌ | 安全/可疑/危险 | ✅/❌ | 批准/拒绝/待确认 |

（无新增依赖则删除本节）

---

## 影响面分析

- **受影响的模块：**
- **受影响的接口：**
- **受影响的配置：**
- **需要同步更新的文档：**

---

## 回滚方案

| 变更类型 | 回滚方式 | 回滚验证 |
|----------|----------|----------|
| 代码修改 | `git revert <commit-range>` | 编译通过 + 测试通过 |
| 新增文件 | `git rm <file>` | 编译通过 |
| 文件删除 | `git checkout <commit> -- <file>` | 文件恢复 + 测试通过 |
| 依赖变更 | 从 package.json/Cargo.toml 移除并重新安装 | 运行正常 |
| 配置变更 | 恢复原始配置值 | 服务启动正常 |
| 数据库变更 | 执行回滚迁移脚本 | 数据完整性检查通过 |

---

## 复跑指令

```bash
# 安装依赖
[安装命令]

# 运行测试
[测试命令]

# 启动服务
[启动命令]
```

---

## 已知遗留

| # | 遗留项 | 严重度 | 计划处理方式 |
|---|--------|--------|-------------|
| 1 | | 严重/重要/次要 | |
