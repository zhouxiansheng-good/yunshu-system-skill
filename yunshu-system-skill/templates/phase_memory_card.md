# phase_memory_card.md（模板）— 阶段记忆卡片（用于上下文压缩/换会话继续）

> 目标：把"到目前为止最关键的信息"压缩成可复跑、可恢复的卡片。
> 约束：**短、结构化、只保留决定性信息**（建议 200–600 字；避免粘贴大段代码/日志）。

## 1) 基本信息（必填）
- task_id：
- run_id：
- checkpoint_id：（建议 `task_id-阶段-时间戳`）
- 当前阶段：澄清 / 计划 / 规范 / 执行 / 验收 / 交付
- 目标（goal，一句话）：

## 2) 进展摘要（3–12 条，必填）
- 

## 3) 关键决策（必填）
- （为什么这么做 / 取舍是什么 / 未来不能轻易改变的假设）

## 4) 未决问题 / 待确认（可为空）
- 

## 5) 下一步行动（可执行清单，必填）
1. 

## 6) 关键引用（必填：用于"换会话后重新加载上下文"）
> 写清楚"要读什么/为什么"，不要把内容复制进来。
- 关键文件：
  - （path）—（为什么要读）
- 关键目录：
  - （path）—（为什么要读）
- 关键命令（如需要复跑/复现）：
  - （command）—（预期输出/通过标准）
- 关键证据（如验收/定位）：
  - （截图/trace/log 路径或链接）—（证明了什么）

## 7) 上下文换挡
当你发现开始遗忘历史决策/反复问相同问题/需要引用大量历史时，执行：
1. 写出本卡片（本文件）+ `phase_memory.json`（包含 task_id / 当前阶段 / 进展摘要 / 关键决策 / 下一步行动）+ `checkpoint.json`（包含 task_id / checkpoint_id / 当前阶段 / 已完成 / 未完成 / 关键产物路径）
2. 在 Trae 新开对话（或清空当前对话）
3. 在新对话里用 `#File` 引用上述 2–3 个文件
4. 对话开头一句话恢复：**"使用云舒系统，从 checkpoint_id=<...> 恢复继续执行；以 phase_memory 为唯一历史事实来源。"**

## 8) 退化信号记录
> 换会话前记录观察到的退化信号，帮助新会话AI理解上下文质量。
- 记忆混乱：[是/否，具体表现]
- 质量下降：[是/否，具体表现]
- 响应变慢：[是/否，具体表现]
- 上下文遗忘：[是/否，具体表现]

## 9) 恢复优先级
> 新会话AI应按此顺序加载上下文。
1. [最高优先级：必须首先读取的文件/信息]
2. [次高优先级]
3. [可选补充]
